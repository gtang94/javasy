package io.github.gtang94.finejar.jdk;

import com.sun.org.apache.xpath.internal.operations.String;
import org.junit.Test;

import java.io.UnsupportedEncodingException;
import java.util.Base64;

/**
 * @author tanggq
 * @class EncryptionOption
 * @description
 * @date 2021/5/6
 **/
public class EncryptionOption {

    @Test
    public void base64Encrypt() throws Exception {
        // 加密
//        String encode = Base64.getEncoder().encodeToString("XJZH2021TJH00001".getBytes("UTF-8"));
//        System.out.println(encode);

//        String encode = Base64.getEncoder().encodeToString("son".getBytes("UTF-8"));
//        System.out.println(encode)

        // 解密
//        byte[] decode = Base64.getDecoder().decode("WEpaSDIwMjFUSkgwMDAwMQ==");
//        System.out.println(new String(decode, "UTF-8"));
    }
}
